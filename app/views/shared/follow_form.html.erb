<% unless current_user == @user %>
  <div class="follow_form">
    <% if current_user.following?(@user) %>
      <%= render template: 'shared/unfollow' %>
    <% else %>
      <%= render template: 'shared/follow' %>
    <% end %>
  </div>
<% end %>
